#include <stdio.h>
#include <unistd.h>
/**
 * main - Entry Point
 * Description: Print possible different combinations of three digits
 * Return: Always 0 (success)
 */
int main(void)
{
	int x, y;

	for (x = '0'; x <= '9'; x++)
	{
		for (y = '0'; y <= '9' y++)
